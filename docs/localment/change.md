The `set()` function sets the value of a variable to ___2___, and the `change()` increases it by ___2___.

```py
variables.set('str', 'abc')
variables.set('bool', True)

variables.change('int', 7)
variables.change('float', 1.2)
```
